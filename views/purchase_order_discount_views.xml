<odoo>
    <!-- Extend Purchase Order Form -->
    <!-- <record id="view_purchase_order_form_discount" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.discount</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total']" position="before">
                <group>
                    <field name="global_discount_percentage" placeholder="Enter Discount (%)"/>
                </group>
            </xpath>
        </field>
    </record> -->

    <!-- Add Global Discount Field -->
    <!-- <record id="global_discount_field" model="ir.model.fields">
        <field name="name">global_discount_percentage</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="field_description">Global Discount (%)</field>
        <field name="ttype">float</field>
        <field name="help">Enter a global discount percentage for the purchase order.</field>
    </record> -->
</odoo>
